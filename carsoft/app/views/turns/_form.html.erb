<%= simple_form_for @turn, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :entry %>
  <%= f.input :exit %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                turns_path, :class => 'btn' %>
  </div>
<% end %>
